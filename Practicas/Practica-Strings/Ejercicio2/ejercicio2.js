"use strict"

let plate = "JKD-3FJ";
let position_plate = (plate.indexOf('-'));
let union;

//posicion del guion 

alert(plate.indexOf('-'));
alert(position_plate);

//obtener pos guion let pos = plate.indexOf('-');

let pos = (plate.indexOf('-'));

//obtener la parte letras: let letras = plate.substring(0, pos);

let letras = place.substring(0, pos);

//obtener la parte numeros: let letras = plate.substring (pos+1);

let numeros = plate.substring(pos+1);

alert(letras);
alert(numeros);

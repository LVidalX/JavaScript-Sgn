'use strict'

//Numbers hacer la suma de la lista

let numbers = [5, 3, 8, 1, 6];

alert(numbers.length);

let suma = 0;

numbers.lenght

for (let i = 0; i < numbers.length; i++ ) {
    suma = suma + numbers[i];
    if(numbers[i] < 5) {
        suma = suma + numbers[i];
        }
    alert(suma)
}
